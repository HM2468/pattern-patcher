-# app/views/lexical_patterns/new.html.haml

%main{ class: "max-w-4xl mx-auto px-6 py-8 space-y-6" }
  %div{ class: "flex items-start justify-between gap-4 px-5" }
    %div
      %h2{ class: "text-2xl font-semibold tracking-tight" } New Pattern
      %p{ class: "mt-1 text-sm text-gray-600" }
        Create a new regex pattern to scan your codebase.
    %div
      = link_to lexical_patterns_path,
        class: "icon-button",
        data: tooltip_data(tip: "Back") do
        = svg_icon("arrow-uturn-left", class_name: "w-6 h-6")

  = render "form"
